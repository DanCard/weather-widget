<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".WeatherWidgetApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherWidget">

        <activity
            android:name=".ui.ConfigActivity"
            android:exported="true"
            android:label="@string/configure_widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="true"
            android:label="@string/settings" />

        <activity
            android:name=".ui.StatisticsActivity"
            android:exported="false"
            android:label="@string/statistics" />

        <activity
            android:name=".ui.FeatureTourActivity"
            android:exported="false"
            android:label="@string/feature_tour" />

        <activity
            android:name=".ui.AppLogsActivity"
            android:exported="false"
            android:label="@string/app_logs"
            android:screenOrientation="fullUser" />

        <activity
            android:name=".ui.ForecastHistoryActivity"
            android:exported="false"
            android:label="@string/forecast_history" />

        <receiver
            android:name=".widget.WeatherWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.weatherwidget.ACTION_REFRESH" />
                <action android:name="com.weatherwidget.ACTION_NAV_LEFT" />
                <action android:name="com.weatherwidget.ACTION_NAV_RIGHT" />
                <action android:name="com.weatherwidget.ACTION_TOGGLE_API" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.ScreenOnReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.intent.action.SCREEN_OFF" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.UIUpdateReceiver"
            android:exported="false" />

        <service
            android:name=".widget.OpportunisticUpdateJobService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
