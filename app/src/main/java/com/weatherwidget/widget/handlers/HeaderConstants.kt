package com.weatherwidget.widget.handlers

/**
 * Shared constants for the widget header to ensure consistency across all views.
 */
object HeaderConstants {
    /**
     * Standard font size for current temperature in DP to ensure it is independent of system font scaling.
     */
    const val CURRENT_TEMP_TEXT_SIZE_DP = 22f
}
